[ IN DEVELOPMENT - NOT READY FOR USE ]

# Cloudbrain Publisher
A GUI for Cloudbrain publishers.

### <a name="install"></a> Installation:

Install dependencies:
```shell
brew tap flysonic10/cloudbrain
brew install cloudbrain
```

For development, run:

```shell
./scripts/load_globals.sh
npm install
npm start
```
